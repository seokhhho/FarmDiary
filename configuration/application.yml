server: 
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/an-sangkil/msa-it-farm.git
          search-paths: configuration
          #password:
          #username:



management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    env:
      enabled: true
